import React from "./node_modules/react";

function customerAdmin() {
  //get customer data

  // display data - name, address(editable), and cart info(ability to remove or change amount)

  return (
    <div>
      <div>
        <h1>customer admin</h1>
      </div>
    </div>
  );
}

export default customerAdmin;
